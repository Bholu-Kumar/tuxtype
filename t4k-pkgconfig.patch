diff --git a/configure.ac b/configure.ac
index 3f42772..b290ff6 100644
--- a/configure.ac
+++ b/configure.ac
@@ -45,16 +45,12 @@ AC_PROG_YACC
 # Checks for libraries.
 # -----------------------------------------------------------------------

-# Check for SDL 1.2
-SDL_VERSION=1.2.0
-
-AM_PATH_SDL($SDL_VERSION, :,
-         AC_MSG_ERROR([SDL version $SDL_VERSION not found! http://www.libsdl.org]) 
-) 
-
-CFLAGS="$CFLAGS $SDL_CFLAGS" 

+# Check for SDL3 satellite libraries and t4k_common via PkgConfig
+PKG_CHECK_MODULES([SDL3], [sdl3 sdl3-image sdl3-ttf sdl3-mixer])
+PKG_CHECK_MODULES([T4KCOMMON], [t4k_common])

+CFLAGS="$CFLAGS $SDL3_CFLAGS $T4KCOMMON_CFLAGS"
+LIBS="$LIBS $SDL3_LIBS $T4KCOMMON_LIBS"

 # Check platform - see if WinMain needed:
 AC_MSG_CHECKING([for native Win32])
